# === Python ===
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/
build/
dist/
*.egg-info/

# === OS & IDE ===
.DS_Store
.vscode/
.idea/
*.swp

# === Environment Variables (절대 업로드 금지!) ===
.env
.flaskenv

# === Machine Learning & Data ===
# 모델 파일은 용량이 크고 바이너리 형태라 깃에 올리지 않는 것이 원칙입니다.
# 팀원과 공유가 필요하다면 LFS를 쓰거나 클라우드를 활용하세요.
models/*.joblib
models/*.pkl
models/*.h5
models/*.onnx

# 데이터셋 (원본 데이터는 용량이 크므로 제외)
# 만약 data 폴더 안의 모든 csv를 제외하고 싶다면 아래와 같이 써도 됩니다.
data/*.csv
!data/raw/.gitkeep
!data/processed/.gitkeep

# === Streamlit ===
.streamlit/config.toml

# === Temporary & Logs ===
logs/
*.log
temp/
.ipynb_checkpoints/


# 추가로 모델 결과물(.pkl)도 git에 올리지 않는 것이 관례입니다 (용량 문제)
models/*.pkl

# 테스트용 파이썬 파일 및 로그파일
generate_test_logs.py
fake_access.log
src/agents/temp_logs/
